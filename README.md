# Keylogger Detection Tool

This tool monitors running processes and flags suspicious ones that may act as keyloggers.

## 🚀 How to Run

1. Install dependencies:
bash
   pip install -r requirements.txt
